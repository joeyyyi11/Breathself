---
description: 部件/模块功能更新后自动 git 提交，便于回滚
alwaysApply: true
---

# 模块更新后 Git 提交

当完成一个**部件或模块的较大功能更新**时，主动帮用户做一次 git 提交，便于后期回滚。

## 何时提交

以下情况视为「较大功能更新」，需要提交：

- 新增或重构某个模块的核心逻辑
- 多个文件联动、影响范围较广的交互/行为变更
- `src/stores/`、`src/composables/` 等底层业务逻辑的重要改动

以下情况**不必**提交：

- 小改：改文案、删/移按钮、微调样式、单点交互调整等
- 纯格式化、注释、重命名且无行为变化
- 用户明确说「先不要提交」「小改不用」或「不要 commit」

## 提交方式

1. 一次功能更新 = 一次提交，按模块粒度拆分（例如 `feat(BreathOrb): ...`、`fix(NoteModal): ...`）
2. 遵循项目既有 commit 风格（`git log` 查看）
3. 遵守 Git Safety Protocol：不 force push、不 amend 已推送提交、不提交 `.env` 等敏感文件
4. 提交前简要告知用户本次提交涵盖的模块与摘要
5. 不要 push，除非用户明确要求

## 示例

完成 `BreathOrb` 呼吸动画逻辑调整后：

```
feat(BreathOrb): 调整吸气/呼气节奏与动画过渡
```
